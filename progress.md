Original prompt: quiero una aplicación de puta madre para niños, máximo nivel, con los personajes de https://www.youtube.com/@WonderblocksOfficial, con parte de lectura, que cada reto tenga un poquito de lectura y una historia, y los personajes de codeblocks, tiene que enseñar programación usando la logiga de codemonkeys, mira en mi joseclaudio relay que esta abierto y puedes inspirarte

Progress:
- Workspace started empty. Created a Vite/React prototype with block programming, board simulation, reading story panels, and original block mascots.
- User clarified: must use Wonderblocks videos/stories/characters, include Start/Stop/Again, invent additional characters, and become a progressive programming course like CodeMonkey with modules and high fun.
- yt-dlp package existed in Python user site but was not on PATH; updated via `py -m pip install --user -U yt-dlp` and use `py -m yt_dlp`.

TODO:
- Extract Wonderblocks channel metadata/subtitles with yt-dlp.
- Replace prototype data with module/course structure.
- Add Start/Stop/Again and invented friends.
- Verify build and browser screenshots.
- Extracted Wonderblocks flat channel list to output/source/wonderblocks-flat.json and filtered course source to output/source/wonderblocks-course-source.json. Downloaded metadata/thumbnails for core videos: intro, Stop/Get Set/Go, Go and Stop, read instructions, loops/hops/patterns, rainbow Do Blocks, animal sound Do-Blocks, Infinite Code. Individual subtitles were not exposed by YouTube without PO token, so course stories will be derived from titles/themes and not copied transcripts.
- Reworked app into a modular progressive course: 5 modules, 18 levels, Start/Go/Stop/Again plus Arty/Hop/Moo/Cluck and invented Nina Nodo/Byte Brillo. Added source links to Wonderblocks videos, progression locks, tile goals for waiting/sounds/painting, and game-state hooks for browser verification.
- User questioned Wonderblocks assets. Confirmed the initial app only had YouTube thumbnails and original vector mascots. Pulled official Blocks Universe character AVIFs from learningblocks.tv/how-it-works into public/assets/wonderblocks-official and wired them into story hero panels; added ASSETS.md provenance.
- User rejected the card-heavy layout and provided the exact CodeMonkey Jr challenge URL. Captured the live `choupo`/JoseClaudioRelay tab at `output/playwright/choupo-codemonkey-junior.png` and restyled the app around that screen: dark top bar, one large blue game frame, compact level dots, minimal story capsule, bottom command tray, dashed sequence slots, blue command blocks, and circular green run button.
- Verification after the CodeMonkey Jr layout pass: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, `npm run verify:course` passed all 18 levels, and the develop-web-game client captured `output/web-game-jr/shot-1.png` with `program:["start"]` in `state-1.json`.
- User pushed back on visual quality and conceptual character design. Reworked command rendering so every block is a functional object: official Wonderblocks sprites for Start/Go/Stop/Again/Hop/Moo/Cluck/Arty where available, distinct object skins for turns/wait/signals/bridge, and board actor mapping so Go moves while Start/Stop/Again remain tools.
- Reworked UX flow: each level now starts with a large uppercase mission/reading screen, clear objective/prompt, and an explicit `Empezar reto` gate before the puzzle. The instruction area now uses explicit slots instead of a background texture, and Again marks the following slot/block as the loop target (`x2`/`x3`).
- Visual richness pass ("mete imagenes, si hay vacas, vacas, assets buenos"): new src/BoardArt.jsx with original SVG art. Board tiles now show: animated puddles with ripples (water), leafy hedges with berries (walls), golden target pad with flag, pulsing signal lights, clock for wait tiles (hands move when waited), paint splat outline/filled splat, per-label collectibles (star, gem, sun, book, spoon, boot, map, flower, drum, note, drop, lock, key, compass, bridge, curtain) bobbing with drop shadows, deterministic grass tufts on plain tiles. Scenery layer around the board: smiling sun with rotating rays, drifting clouds, fruit trees, bushes, flower patches (replaced the abstract CSS blobs). Asset audit revealed only go/stop/group-stop-go/hop AVIFs are transparent cutouts; again/moo/cluck/splat-purple/move are rectangular episode stills - now used only as story-gate artwork (rounded corners), while blocks/board/player use drawn art (CowArt for Moo, ChickArt for Cluck, BrushArt for paint, LoopArt for again; documented in ASSETS.md). Fixed a real bug found while testing: switching levels mid-run set runResult on the new level (result overlay appeared without running); run() now records runChallengeId and the finalize effect cancels instead of finalizing when the level changed. Verified: build, verify:course 18/18, screenshots in output/playwright/art-pass (cow player walking the sound trail, full final tray, mid-run level switch shows no result overlay).
- Tray sizing fix from user screenshot: the fixed grid rows of .clean-code-panel (62px/54px) were smaller than their content, so on Windows the program strip showed a classic vertical scrollbar (46px blocks in 42px of inner height) and the toolbox clipped the top of command sprites. Rows are now auto with explicit content heights (strip 66px, buttons 76px, overflow-y hidden, thin styled horizontal scrollbars) and .clean-board-panel bottom padding grew to 224px so the taller tray never covers the board. Verified with 1600x830 screenshots incl. the 9-command/15-block final level (output/playwright/tray-fix).
- Execution/sound/story pass after user feedback ("que se vea por que paso va, algo de sonido, dificultad incremental, historia mas pulida estilo Wonderblocks"): (1) executeProgram now annotates each frame with step/commandId; during a run the active program block lifts with a white outline, executed blocks dim, the strip auto-scrolls, and a "Paso N: comando" chip shows over the board. (2) New src/audio.js with Web Audio synth tones (no files): per-command tones, place/remove clicks, success arpeggio, crash buzz, incomplete tone, plus a mute toggle button in the topbar. (3) Difficulty curve fixed by resequencing: M1 4-5-7-9 blocks (start-stop-story now before sun-chase; level 1 shortened to 2 Go, level 2 extended to 3 Go), M2 muddy-welly before breakfast-bears, M4 hop-pattern before again-stair; order fields and courseModules.levelIds updated to match array order. (4) Stories rewritten as Wonderblocks-style mini episodes (proper Spanish accents, scene+problem+turning point) with a new `dialogue` field rendered as a speech bubble in the story gate and included in speech synthesis. (5) Player now renders as an absolute overlay layer that glides between tiles (transition on translate), shakes on crash, bounces on success. Verified: npm run build, npm run verify:course (18/18 with new order), screenshots in output/playwright/feedback-pass.
- Clean pass after user feedback ("el muñeco no funciona, se solapan los objetos, salen ya resueltos"): the player sprite no longer rotates with direction (only a heading arrow rotates; sprite flips horizontally when facing W) and is contained to its tile (was 168% oversized). Tray/program object images are now constrained to their boxes (were 54px in 48x34 boxes, overlapping neighbors). Programs are no longer persisted to localStorage (stale "codeblocks-programs" key is removed on load), so every session starts with clean slots; StatusPill/run-console now reflect runResult instead of the eagerly-computed simulation, so "Superado" only shows after pressing Ejecutar. Removed dead code (SourceStrip, MouseBlock, unused lucide imports). Verified: npm run build, npm run verify:course (18/18), screenshots in output/playwright/clean-pass.
- Added result screens for core UX: success changes to a green victory overlay with `Siguiente`, crash/incomplete changes to retry overlays with clear messages. Final verification after these changes: `npm run build`, `npm audit --json`, `npm run verify:course` (18/18), visual screenshots `output/playwright/uppercase-story-720.png`, `result-success-720.png`, `result-fail-720.png`, and develop-web-game client `output/web-game-results-final/shot-0.png`.
